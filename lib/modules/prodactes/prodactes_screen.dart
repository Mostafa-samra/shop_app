import 'package:flutter/material.dart';

class ProductesScreen extends StatelessWidget {
  const ProductesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('productesScreen');
  }
}
